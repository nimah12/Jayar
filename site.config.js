/* جایار — پیکربندی سایت
   SITE_URL: دامنهٔ اصلی سایت؛ برای بازتولید تگ‌های SEO (canonical، og:url، og:image)
   استفاده می‌شود. برای تغییر دامنه، فقط این مقدار را عوض کنید و `npm run build:meta`
   را اجرا کنید تا هر ۸ صفحه به‌طور خودکار بازنویسی شوند. */

module.exports = {
  SITE_URL: 'https://jayar.example',
};
